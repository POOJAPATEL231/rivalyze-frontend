/** The one row Compare needs that Dashboard doesn't already have — a
 * counter-positioning note per rival. Everything else (price, positioning,
 * recent move, top complaint, sentiment) is reused straight from Report. */
export const YOUR_ANGLE: Record<string, string> = {
    northwind:
        "Match the bundle without the bloat — position as the lean, evidence-backed alternative to Northwind's enterprise-first suite.",
    vantage:
        "Lead with reliability — Vantage's post-layoff support decline is your opening on responsiveness.",
    pulsemetrics:
        "Undercut on completeness — PulseMetrics tracks sentiment alone, you track the whole competitive picture.",
    beacon: "Move first and go real-time — Beacon's static reporting can't match live signal detection.",
};
